# Workflow example for web access log

## Usage

```
$ td wf push lda_wf_web_access_log

$ td wf secrets --project lda_wf_web_access_log --set python_apikey

$ td wf start lda_wf_web_access_log lda --session now
```
